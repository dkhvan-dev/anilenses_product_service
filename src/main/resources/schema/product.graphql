# ====================================================
# =                 QUERY                            =
# ====================================================
extend type Query {
    products(category: ProductCategoryEnum!, pageable: PageableInput!): ProductPage!
}

# ====================================================
# =                 TYPES                            =
# ====================================================
type ProductPage {
    "Информация о товарах"
    content: [ProductInterface]!

    "Общее количество"
    totalElements: Long!
}

# ====================================================
# =                 ENUMS                            =
# ====================================================
enum ProductCategoryEnum {
    LENSES
}